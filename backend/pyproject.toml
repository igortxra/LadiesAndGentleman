[project]
name = "backend"
version = "0.1.0"
description = "Project for backend challenge called 'Rinha de Backend'"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "redis>=6.2.0",
    "uvicorn>=0.35.0",
]
