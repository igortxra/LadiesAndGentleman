version: "2.4"

services:
  nginx:
    image: nginx:alpine
    ports:
      - "9999:9999"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    networks:
      - backend
    depends_on:
      backend-1:
        condition: service_healthy
      backend-2: 
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.25' # Não mexer aqui
          memory: '15MB' # Não mexer aqui
    ulimits:
      nofile:
        soft: 100000
        hard: 100000

  backend-1:
    build:
      context: ./backend
      dockerfile: Dockerfile
    networks:
      - backend
      - payment-processor
    environment:
      - REDIS_URL=redis://redis:6379
    deploy:
      resources:
        limits:
          cpus: "0.2"
          memory: "140MB"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://backend-1:9999/health"]
      start_period: 40s
      timeout: 1s
      interval: 10m
      retries: 3
    depends_on:
      - redis
      - worker

  backend-2: 
    build:
      context: ./backend
      dockerfile: Dockerfile
    networks:
      - backend
      - payment-processor
    environment:
      - REDIS_URL=redis://redis:6379
    deploy:
      resources:
        limits:
          cpus: "0.2"
          memory: "140MB"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://backend-2:9999/health"]
      start_period: 40s
      timeout: 1s
      interval: 10m
      retries: 3
    depends_on:
      - redis
      - worker

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    networks:
      - backend
      - payment-processor
    command: redis-server --maxmemory 15mb --maxmemory-policy allkeys-lru --save ""
    deploy:
      resources:
        limits:
          cpus: '0.1'
          memory: '15MB'

  worker:
    build:
      context: ./worker
      dockerfile: Dockerfile
    networks:
      - backend
      - payment-processor
    environment:
      - PAYMENT_PROCESSOR_URL_DEFAULT=http://payment-processor-default:8080
      - PAYMENT_PROCESSOR_URL_FALLBACK=http://payment-processor-fallback:8080
      - REDIS_URL=redis://redis:6379
    deploy:
      resources:
        limits:
          cpus: "0.75"
          memory: "40MB"
    depends_on:
      - redis

networks:
  backend:
    driver: bridge
  payment-processor:
    external: true 
